// vong lap tu 1 den 100
for (let i = 1; i <= 100; i++) {
    if (i % 2 === 0) { console.log("So " + i, "la so chan") } // in ra so i la so chan neu i chia het cho 2
    if (i % 2 === 1) { console.log("So " + i, "la so le") } // in ra so i la so le neu i khong chia het cho 2
};